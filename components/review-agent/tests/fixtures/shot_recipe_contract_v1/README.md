# Shot recipe contract fixture v1

This is a synthetic, read-only compatibility fixture for Task5. It is not Task4's final schema and must not be used to claim overall production support. `candidate.bin` is represented by the documented SHA in the JSON fixtures; unit tests generate exact-SHA temporary candidates for executable coverage.
