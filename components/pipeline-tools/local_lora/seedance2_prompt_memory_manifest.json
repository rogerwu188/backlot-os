{
  "dataset": "seedance2_prompt_failure_training.jsonl",
  "dataset_sha256": "679d432f696d2fba21d6e64e84ca1bd117497be7b483a22518d98eb79a9d3f29",
  "deployment": "The dataset ships with BacklotOS and is loaded relative to seedance2_prompt_compiler.py.",
  "format": "LoRA-ready JSONL plus deterministic compiler retrieval",
  "hard_rules": [
    "Conflicting reference geometry is removed; prose-only negative isolation is insufficient.",
    "A unique story prop has one named owner; every non-owner receives a distinct visible empty-hand action.",
    "Every adjacent keyframe pair declares a physically reachable camera side, position, facing and continuous path before paid generation.",
    "Generated text surfaces stay blank and exact glyphs are added only as real-font AgentCut layers.",
    "Exact dialogue glyphs stay out of visual prompts; visible speech keeps bound native audio and lip sync, while deliberate closed-mouth voice-over is labeled separately.",
    "Three-beat action long takes require at least three ordered state-authority frames with actor, contact and body-relative effect-scale locks.",
    "After an accepted predecessor locks the full roster, the next state image uses it as the sole state authority and declares an exact actor count before any changed action.",
    "Multi-character video prompts require a visible-actor motion ledger with per-actor paths, interactions, terminal positions and sub-second displacement checks; held poses, gesture loops and frozen backgrounds are rejected.",
    "Text-layer-post-only shots run PROMPT_LITERAL_GLYPH_SCAN; exact audience-facing strings are replaced with opaque PROP_IDs before provider generation.",
    "Every unit declares ON_CAMERA_NATIVE_LIP_SYNC, CLOSED_MOUTH_VOICE_OVER, or NO_DIALOGUE; contradictory silent-performance and on-camera-speech contracts fail before submission."
  ],
  "runtime_behavior": "Compilation loads admitted samples and pending defensive rewrites applicable to the requested image or video mode; pending records cannot claim a successful accepted asset.",
  "sample_count": 10,
  "schema": "backlotos.seedance_prompt_memory_manifest.v1",
  "sync_policy": "PRIVACY_FILTERED_CONTENT_ADDRESSED_GITHUB_CONVERGENCE",
  "training_status": "RULE_ADAPTER_ACTIVE_WEIGHT_TRAINING_NOT_CLAIMED"
}
